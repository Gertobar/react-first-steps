export const sumary = (a: number, b: number) => {

    return a + b;
}

export const subtract = (a: number, b: number) => {
    return a - b;
}

export const muliply = (a: number, b: number) => {
    return a * b;
}

export const divide = (a: number, b: number) => {
    return a / b;
}